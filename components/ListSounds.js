import { View, Button, Text, ScrollView, FlatList } from "react-native";


const ListSounds = () => {

    return (
        <View style={{flexDirection : "column", justifyContent:"center", alignItems:"center"}}>
            <Text> My sounds list </Text>
        </View>
    );
  };

export default ListSounds;